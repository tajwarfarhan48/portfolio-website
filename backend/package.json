{
  "name": "portfolio-website-backend",
  "version": "1.0.0",
  "description": "Backend for my portfolio website. Implements email sending functionality",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:frontend": "rm -rf build && cd ../frontend && npm run build && cp -r build ../backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Farhan Tajwar Romit",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.259.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
